<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="me.jerryhanks.todo.ui.FirstFragment"
        android:label="@string/first_fragment_label"
        tools:layout="@layout/fragment_first"/>
    <fragment
        android:id="@+id/accountFragment"
        android:name="me.jerryhanks.todo.ui.SecondFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_second"/>
    <fragment
        android:id="@+id/timelineFragment"
        android:name="me.todo.todo.timeline.TimelineFragment"
        android:label="TimelineFragment"
        tools:layout="@layout/timeline_fragment" />
    <activity
        android:id="@+id/newTaskActivity"
        android:name="me.todo.createtaskfeature.ui.NewTaskActivity"
        android:label="activity_new_task"
        app:moduleName="createTaskFeature"
        tools:layout="@layout/activity_new_task" />
</navigation>